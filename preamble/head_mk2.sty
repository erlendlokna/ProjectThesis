

% -----------------------------------------------------------------
% Packages for the document
% -----------------------------------------------------------------
\usepackage{afterpage}  % Execute command after the next page break


% ------------------------------------------------------------------
% Typesetting
% ------------------------------------------------------------------
\usepackage{typearea}

\usepackage{setspace}  % Set space between lines (\onehalfspacing, doublespacing)
\usepackage{rotating}  % Allows for rotating pages/figures etc
\usepackage{subfiles}  % Allows for subfile functionality


% ------------------------------------------------------------------
% Citation, bibliography, todo and markings
% ------------------------------------------------------------------
\usepackage[toc,page]{appendix}  % for making an appendix
%\setcounter{chapter}{+1}
%\usepackage{bibtopic} % Several bibtex files within one document


% ------------------------------------------------------------------
% Hyperlinks and References
% ------------------------------------------------------------------
% test om denne funker
\usepackage{url}  % prints URLs like they should be in bibtex


% ------------------------------------------------------------------
% Tabular and arrays
% ------------------------------------------------------------------
\usepackage{tabularx}
\usepackage{booktabs}  % Is needed for \toprule f.e. in tables
\usepackage{array}  % Extending the array and tabular environments


% ------------------------------------------------------------------
% Floating environments
% ------------------------------------------------------------------
\usepackage{wrapfig}  % Floating figures
%\usepackage{subfig}

%\usepackage{float}  % Do not use this if using floatrow
\RequirePackage{floatrow}
%\floatsetup[figure]{style=plain}
%\floatsetup[table]{style=plaintop}

%\RequirePackage{sansmath}
% \DeclareCaptionFont{sansmath}{\sansmath}


% ------------------------------------------------------------------
% Graphics, colors etc
% ------------------------------------------------------------------
% \graphicspath{{images/}{../images/}}  % example for more folders
\graphicspath{{./figures/figure-png}}



% ------------------------------------------------------------------
% Abstract environment
% ------------------------------------------------------------------
%\RequirePackage{abstract}
%\renewcommand{\abstractnamefont}{\normalfont\bfseries}
%\renewcommand{\abstracttextfont}{\normalfont}
%%\renewcommand\abstractname{Abstract} 
%\renewenvironment{abstract}{
%	\par
%	\centering
%	\begin{minipage}{0.9\textwidth}
%		\textbf{\large\centering\abstractname\\[2ex]}
%	}{
%   \end{minipage}\\[10ex]
%}


% ------------------------------------------------------------------
% Mathematical expressions
% ------------------------------------------------------------------
\let\openbox\relax
\let\Bbbk\relax

% resten av amsmath er allerede lastet inn
%\usepackage{amsfonts}  % Bruker modern font encoding
\usepackage{amscd}      % Commutative diagrammer
\usepackage{mathtools}  % Fixes some amsmath quirks
\usepackage{physics}  %  Allows for dirac notation
\usepackage{wasysym}  % Allows for star signs and etc
\usepackage{gensymb}  %  Allows use \degree
%\usepackage{upgreek}  %  Upright Greek letters
\usetikzlibrary{matrix, positioning}
\usetikzlibrary{patterns}
\usetikzlibrary{positioning, shapes.geometric, arrows.meta, calc}


\numberwithin{equation}{chapter}
\RequirePackage{nicefrac}
\RequirePackage{chemformula}
%\usepackage{proof}  %  proof enviroment

% ------------------------------------------------------------------
% DIV
% ------------------------------------------------------------------
\usepackage{adjustbox}
\usepackage{multirow}  % multirows
\usepackage{multicol}  % multicolumns

\usepackage{listings}  % for kodesnippets

%\usepackage{dirtytalk}  % For quotations. One command: \say{}, will also nest quoations  % Not needed, uses csquotes
%\usepackage[  % q is ' and  qq is '' while l and r is left and right respectivly
%   left     = \lq{},  %  g in front, i.e \glqq{} for german notation -> „ text “
%   right    = \rq{},  %  f in front, i.e \flqq{} for french notation -> « text »
   %leftsub  = \lqq{}, 
   %rightsub = \rqq{}
% ]{dirtytalk}  % For quotations. One command: \say{}, will also nest quoations



\usepackage[bb=ams,
frak=mma,
frakscaled=.97,
scr=rsfs]{mathalpha}


\usepackage{bbm}  % For indicator function
%\usepackage{dsfont} alternative math fonts? for $\mathds{1}$

%\usepackage{tocloft}


% For text with strikethrough and similar
\usepackage{cancel}
% \cancel{text to cancel} draws a diagonal line (slash) through its argument
% \bcancel{text to cancel} uses the negative slope (a backslash)
% \xcancel{text to cancel} draws an X (actually \cancel plus \bcancel)
% \cancelto{〈value〉}{〈expression〉} draws a diagonal arrow through the 〈expression〉pointing to the 〈value〉 (math-mode only)

\usepackage[normalem]{ulem}  % The problem that ulem affects some bibliography styles where otherwise italicised text is then underlined can be remedied through the »normalem«-option in the preamble
% \sout{text to be striked out}
% \xout{text to be crossed out}
\usepackage{amsmath} % include this in your preamble
\mathcode`;=\numexpr\mathcode`;-"6000

\DeclareMathOperator*{\argmin}{arg\,min} % define argmin
\DeclareMathOperator*{\argmax}{arg\,max} % define argmax
\newcommand{\Lagr}{\mathcal{L}}

\usepackage[utf8]{inputenc}
\usepackage{amsmath, amssymb, latexsym}
\DeclareMathOperator{\ReLU}{ReLU}
\newcommand{\KL}{\ensuremath{\mathrm{KL}}}
\newcommand{\Ber}{\ensuremath{\mathrm{Ber}}}
\usepackage{todonotes}
\usepackage{tikz}
\usepackage{xcolor}
\usepackage{xspace}
\usepackage[utf8]{inputenc}
\usepackage{amsmath, amssymb, latexsym}

\usepackage{lipsum}
\usepackage[Lenny]{fncychap}
